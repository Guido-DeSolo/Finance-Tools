[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "finance-tools-technical-indicators"
version = "1.0.0"
description = "Dependency-free technical analysis indicators"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["technical_indicators*"]
